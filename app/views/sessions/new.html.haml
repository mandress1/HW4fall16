%h1 Login

= form_tag session do

  = label :user, :user_id, 'User-ID'
  = text_field :user, :user_id
  
  = label :user, :email, 'E-Mail'
  = text_field :user, :email
  
  = submit_tag 'Login to my account'
  
%br
= link_to 'Sign-up for an accout', new_user_path